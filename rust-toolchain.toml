# Pinned to an exact version (not "stable") so a `nix flake update` of the
# fenix input can't silently change rustfmt/codegen behaviour. Bump
# deliberately and re-run `cargo fmt` in the same commit.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy", "rust-src"]
