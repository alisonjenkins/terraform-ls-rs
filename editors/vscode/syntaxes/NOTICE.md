# Vendored grammar

`terraform.tmGrammar.json` is vendored from
[hashicorp/syntax](https://github.com/hashicorp/syntax) (tag v0.7.1),
licensed under MPL-2.0 — the same license as this project.

Scope name: `source.hcl.terraform`. Update by re-downloading
`syntaxes/terraform.tmGrammar.json` from a pinned hashicorp/syntax tag.
